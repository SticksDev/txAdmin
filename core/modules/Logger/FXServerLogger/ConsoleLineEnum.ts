//In a separate file to avoid circular dependencies
enum ConsoleLineEnum {
    StdOut,
    StdErr,
    MarkerAdminCmd,
    MarkerSystemCmd,
    MarkerInfo,
    MarkerWarning,
}

export default ConsoleLineEnum;
